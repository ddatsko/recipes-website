import React from "react";

export default class Recipe extends React.Component {
    constructor(props) {
        super(props);
        console.log(props)
    }

    render() {
        // TODO: create components for the specific recipe display
        return (
            <></>
        )
    }

}